export const styles = {
  container: (theme) => ({}),
  image_next_text: (theme) => ({
    display: "flex",
    justifyContent: "flex-start",
    mb: 2,
    gap: 1,
    fontSize: "21px",
  }),
  input: (theme) => ({
    width: "300px",
    [`& fieldset`]: {
      borderRadius: "8px",
    },
  }),
};
