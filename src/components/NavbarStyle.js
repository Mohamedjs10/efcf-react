export const styles = {
  container: (theme) => ({
    display: "flex",
    gap: 1,
    justifyContent: "space-around",
    alignItems: "center",
    // position: "fixed",
    // top: "10px",
  }),
  navbar_container: (theme) => ({
    display: "flex",
    alignItems: "center",
    gap: 2,
  }),
  logo_container: (theme) => ({
    width: "350px",
    height: "100px",
  }),
};
