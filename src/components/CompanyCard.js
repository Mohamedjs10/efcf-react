export const styles = {
    container: (theme) => ({
        border: "1px solid #e2e2e2",
        borderRadius: "10px",
        padding: "10px",
        overflow: "hidden",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
    })
}