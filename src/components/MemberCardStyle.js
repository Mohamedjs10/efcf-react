export const styles = {
  container: (theme) => ({
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
  }),
  img: (theme) => ({
    border: "1px solid #d5d5d5",
    borderRadius: "14px",
  }),
  text: (theme) => ({
    textAlign: "center",
    fontSize: "20px",
    fontFamily: "cairo",
    mt: 2,
  }),
};
